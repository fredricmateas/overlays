
int main(){

Printf ("transfer to exelp function \n"); 
execll ("./oup","jour", NULL) Prints (this line will not execute(""); return 0;
}
int main() {

Printf ("transfer to execlp function \n") 
char * arr[] = { head", "head", "-")", "fi", NULL)}; 
execl("./oup", arr);

Print ("this line will not execution")

Return 0;



int main() {

        Printf("transfer to exelp function \n");
        execll("./oup", "jour", NULL)
        Prints(this line will not execute("");
            return 0;
        }


int main() {

Printf ("transfer to execlp function \n") 
char * arr[] = { head", "head", "-")", "fi", NULL)}; 
execl("./oup", arr);

Print ("this line will not execution")

Return 0;